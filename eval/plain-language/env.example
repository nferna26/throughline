# Live tutor backends (run_eval.py)
ANTHROPIC_API_KEY=
LOCAL_AI_BASE_URL=http://localhost:11434/v1

# Cross-family judge (calibrate.py --judge local). Must NOT be the tutor family
# (Sonnet) nor the local tutor (qwen3.5). Defaults below; override as needed.
# JUDGE_BASE_URL=http://localhost:11434/v1
# JUDGE_MODEL=gpt-oss:20b
# GEMINI_API_KEY=        # for --judge gemini
# OPENAI_API_KEY=        # for --judge gpt
