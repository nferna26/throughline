# Plain-language eval harness (CORE-1169 part 1). Offline lexical stack.
# Pin loosely; the gate is deterministic given a frozen wordfreq data snapshot.
wordfreq>=3.0           # word rarity (Zipf), bundled msgpack data, offline after install
textstat>=0.7           # Dale-Chall familiar-word list + classic formulas (advisory)
simplemma>=1.0          # lightweight offline lemmatizer (no model download)
numpy>=1.24
scipy>=1.10             # Spearman (calibration)
scikit-learn>=1.3       # Cohen's kappa (calibration)
openpyxl>=3.1           # only to parse a Kuperman AoA .xlsx if you fetch one (optional)
requests>=2.31          # only for download_lexicons.py (optional AoA fetch)
