# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — signing credentials, app-specific passwords, exported certs.
# NEVER commit these.
.env
.env.*
*.p12
*.p12.b64

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only research + agent tooling (not product)
local-only/
# Local agent scratch stays out; the shared project config is tracked.
.claude/*
!.claude/settings.json
.codex/

# Self-verify UI harness artifacts
e2e/shots/
e2e/.output/
test-results/
playwright-report/
